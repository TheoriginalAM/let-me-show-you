/** A small "Recording" status pill for window mode, where a region can't be outlined. */
export function RecordingPill() {
  return (
    <div className="rec-pill">
      <span className="rec-pill-dot" />
      Recording
    </div>
  )
}
